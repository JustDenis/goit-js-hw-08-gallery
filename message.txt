Я реализовала пока без дата атрибутов. Через map сделала массив больших форматов картинок, которые нужны для модалки. Создала функцию, которая получает аргументом значение 'left' или 'rigth' (а точнее 'ArrowLeft' и 'ArrowRight', это соответствует event.code который будет браться в хендлере) и через reduce получаю url следующей или предыдущей картинки, в зависимости от аргумента, который придет при вызове. И перезаписываю глобальную переменную, созданную выше, какую-нибудь let imageUrl; (edited) 


В хендлере проверяю event.code и чтоб индекс картинки был в пределах массива, т.е. не переходил в null, и вызываю функцию описанную выше, куда и передаю event.code. После чего присваиваю его атрибуту src, так как писала выше modalImage.src = imageUrl.

Привет! Делала так. асс в reduce это "". Проверяю если item равен imageUrl (data-sourse картинки которая в данный момент в модалке). Дальше если value (значение которое приходит в функцию) right - acc=images[i+1], соответственно если left то acc=images[i-1]. И возвращаю acc. Как-то так)

Да, imageUrl это у меня глобальная переменная. Глобальная для того чтоб был к ней доступ в других функциях. Я вписываю в нее url текущей картинки при открытии модалки. А при перелистывании, сравниваю items (каждая картинка в редюсе) с этим url и перезаписываю его.